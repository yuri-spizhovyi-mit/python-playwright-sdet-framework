[pytest]
testpaths = apps api
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    smoke: Quick smoke tests
    full: Full regression suite
    flaky: Tests with known intermittent issues
    api: API tests

addopts = 
    -v 
    --tb=short
    --strict-markers